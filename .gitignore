# ---- Unity -------------------------------------------------------------
# Regenerated from Assets/ and ProjectSettings/ on open; huge and machine-local.
/unity/Library/
/unity/Temp/
/unity/Obj/
/unity/Logs/
/unity/UserSettings/
/unity/Build/
/unity/Builds/
/unity/MemoryCaptures/
/unity/Recordings/

# Unity regenerates these from the asmdefs; the server's own csproj is source.
/unity/*.csproj
/unity/*.sln

# ---- .NET / SpacetimeDB module -----------------------------------------
server/module/bin/
server/module/obj/

# ---- Local overrides ---------------------------------------------------
# Points at one developer's cloud database.
server/spacetime.local.json

# ---- Manual backups ----------------------------------------------------
# Kept on disk, not in history.
/.backup-pre-urp/

# ---- OS ----------------------------------------------------------------
.DS_Store
*.swp

# ---- Licensed third-party ----------------------------------------------
# Odin Inspector is per-seat commercial software and must not be redistributed.
# Nothing in this project references it — no code, asmdef, scene or asset — so
# a clone compiles without it. Reinstall from the Asset Store if you want it.
# The whole folder, meta included: Sirenix is its only contents, so tracking
# the meta alone would leave Unity warning about metadata with no asset.
/unity/Assets/Plugins/
/unity/Assets/Plugins.meta

# ---- IDE -----------------------------------------------------------------
# Rider workspace state (project ids, local changelists, usage stats), which it
# drops into any folder opened on its own, including inside Assets.
.idea/
